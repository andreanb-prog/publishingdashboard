Drop the Fetch doggy icon here as THREE PNG files:

  dog-16.png    16 x 16 px   (toolbar)
  dog-48.png    48 x 48 px   (extensions menu + popup header)
  dog-128.png   128 x 128 px (Chrome Web Store + install)

Export the doggy from your "Fetch Doggy.html" design at each size.
Square, transparent background. The manifest and popup already reference
these exact filenames, so once they're here the extension shows the dog.

Until the PNGs are added, Chrome will show a default puzzle-piece icon —
the extension still works, it's just cosmetic.
